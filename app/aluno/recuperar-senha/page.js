'use client';
import {useState} from 'react';
import {getSupabaseBrowserClient} from '../../../lib/supabase';
export default function Recuperar(){const[email,setEmail]=useState('');const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function enviar(e){e.preventDefault();setBusy(true);setMsg('');try{const supabase=getSupabaseBrowserClient();const {error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/aluno/redefinir-senha`});if(error)throw error;setMsg('Se existir uma conta com este e-mail, você receberá as instruções para redefinir a senha.');}catch(e){setMsg('Não foi possível enviar a solicitação agora. Tente novamente.');}finally{setBusy(false);}}return <main><header className="header"><a className="brand" href="/"><b>ACADEMIA CAA</b><span>Formação para Transformar Nações</span></a></header><section className="checkoutPage"><div className="checkoutIntro"><p className="gold">RECUPERAR ACESSO</p><h1>Esqueceu sua senha?</h1><p>Informe o e-mail cadastrado na Academia CAA.</p></div><form className="formCard" onSubmit={enviar}><label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/></label><button className="button" disabled={busy}>{busy?'Enviando...':'Enviar instruções'}</button>{msg&&<p className="safe" role="alert">{msg}</p>}<p className="safe"><a href="/aluno">Voltar para entrar</a></p></form></section></main>}
