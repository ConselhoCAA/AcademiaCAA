'use client';
import {useState} from 'react';
import {getSupabaseBrowserClient} from '../../../lib/supabase';
export default function Redefinir(){const[password,setPassword]=useState('');const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function salvar(e){e.preventDefault();setBusy(true);setMsg('');try{const supabase=getSupabaseBrowserClient();const {error}=await supabase.auth.updateUser({password});if(error)throw error;setMsg('Senha atualizada. Você já pode acessar sua Área do Aluno.');}catch(e){setMsg('Não foi possível redefinir a senha. Abra novamente o link recebido por e-mail e tente outra vez.');}finally{setBusy(false);}}return <main><header className="header"><a className="brand" href="/"><b>ACADEMIA CAA</b><span>Formação para Transformar Nações</span></a></header><section className="checkoutPage"><div className="checkoutIntro"><p className="gold">NOVA SENHA</p><h1>Redefina seu acesso</h1><p>Crie uma nova senha para sua conta.</p></div><form className="formCard" onSubmit={salvar}><label>Nova senha<input type="password" minLength={6} value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="new-password"/></label><button className="button" disabled={busy}>{busy?'Salvando...':'Salvar nova senha'}</button>{msg&&<p className="safe" role="alert">{msg}</p>}<p className="safe"><a href="/aluno">Ir para a Área do Aluno</a></p></form></section></main>}
